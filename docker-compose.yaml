services:
  pyfractal:
    build:
      context: .
      dockerfile: pyfractal.dockerfile
    environment:
      - PROGRAM_NAME=main
      - DATA_DIR=/app/data
      #- DATASET=soybean_ppigremlin.txt
    volumes:
      - .:/app
    command: sh run_metadata_with_different_datasets.sh
